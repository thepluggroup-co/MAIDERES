{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @maideres/web build",
  "outputDirectory": "dist",
  "installCommand": "echo skip",
  "rewrites": [
    { "source": "/((?!assets/|api/).*)", "destination": "/index.html" }
  ]
}
