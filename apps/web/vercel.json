{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @forge/web build",
  "outputDirectory": "dist",
  "installCommand": "echo skip",
  "rewrites": [
    { "source": "/((?!assets/|api/).*)", "destination": "/index.html" }
  ]
}
